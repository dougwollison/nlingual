The controls on this screen affect basic functionality of site translation; namely the URLs for translated versions of pages on the site.

By default, objects of all languages are visible together in the admin. This can be turned off if desired, and all objects of the default language (or no language at all) will be listed unless otherwise specified).
